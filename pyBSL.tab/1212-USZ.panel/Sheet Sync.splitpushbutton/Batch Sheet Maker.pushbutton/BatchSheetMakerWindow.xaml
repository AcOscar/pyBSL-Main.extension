<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        ShowInTaskbar="False"
        Width="600" Height="500" ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterScreen"
        HorizontalContentAlignment="Center"
		Title="1212 USZ - Batch Sheet Maker">
        <DockPanel Margin="10">
            <TextBlock DockPanel.Dock="Top">
                Enter list of sheet number and names in this format:<LineBreak />
                <Bold>sheet-number</Bold>  TAB  <Bold>sheet-name</Bold> (e.g. A311 <Run FontSize="18" Text="⇥"/> SHEET NAME)<LineBreak />
                <Bold>Range of Sheet Numbers:</Bold>   e.g. ARC_103--ARC_106 <LineBreak />
                <Bold>List of Sheet Numbers:</Bold>   e.g. ARC_104,ARC_105 or ARC_104;AARC_105<LineBreak />
				    or just: <LineBreak />
				<Bold>sheet-number</Bold> as SHEET NAME the number will be used

            </TextBlock>
            <StackPanel DockPanel.Dock="Bottom">
                <WrapPanel>
                    <RadioButton x:Name="sheet_cb" GroupName="sheetoptions"
                                 Margin="10,10,0,5"
                                 IsChecked="True">
                        Create Sheets
                    </RadioButton>
                    <RadioButton x:Name="placeholder_cb"
                                 GroupName="sheetoptions"
                                 Margin="10,10,0,5"
                                 IsChecked="False">
                        Create Placeholders
                     </RadioButton>
                    <CheckBox x:Name="idparam_cb"
                                 
                                 Margin="10,10,0,5"
                                 IsChecked="True">
                        Set Number as Plan__ID parameter
                     </CheckBox>
                </WrapPanel>
                <Button Content="Create Sheets" Margin="0,10,0,0"
                        Height="24"
                        Click="create_sheets"/>
            </StackPanel>
            <TextBox x:Name="sheets_tb"
                     AcceptsReturn="True"
                     AcceptsTab="True"
                     Margin="0 10 0 0"
                     FontFamily="Courier New" FontSize="14"
                     ScrollViewer.CanContentScroll="True"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"/>
        </DockPanel>
</Window>
